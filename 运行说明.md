# 医院门诊管理系统运行说明

对标小型 HIS 门诊业务：患者档案、挂号、医生排班、处方开具、收费退费、药房发药、检验计费、诊断管理一体化 Web 后台。
技术栈：Python Flask + MySQL 8.4 + 黑盒测试（禅道用例）+ XMind 需求设计。

## 一、一分钟速览

| 步骤 | 命令 | 说明 |
| --- | --- | --- |
| 1. 启动 MySQL | 双击项目根目录 `start_mysql.bat` | 免服务方式，root / 123456 |
| 2. 初始化数据库 | `python init_database.py` | 一键执行 sql/01~11，可重复运行 |
| 3. 启动 Web | 双击 `run_web.bat` | 浏览器打开 http://127.0.0.1:5000 |
| 4. 登录体验 | admin01 / 123456 | 管理员全权限账号（可注册新账号） |
| 5. 检验科账号 | lab01 / 123456 | 检验技师（检验申请/结果录入/汇总） |
| 6. 药房账号 | pharm01 / 123456 | 药房人员（已收费处方发药） |
| 7. 挂号机账号 | regm01 / 123456 | 自助挂号终端（身份证识别/自助建档/自助挂号，登录后直达终端页） |

## 二、文件结构

```
医院信息系统设计开发/
├── sql/                          # 数据库脚本（01 建库 ~ 07 测试数据，08 检验科+体征，09 任务书补全，10 预约挂号，11 公告）
├── web_app/                      # Flask Web 后端
│   ├── app.py                    # 全部业务路由（登录/患者/挂号/排班/处方/诊断/收费/发药/药品/报表/用户/公告/科室）
│   ├── db.py                     # MySQL 连接配置（默认 root/123456，可用环境变量覆盖）
│   ├── templates/                # 18 个页面模板（新增 announcements、departments）
│   └── static/style.css          # 样式
├── docs/
│   ├── 黑盒测试用例_医院门诊管理系统.xlsx   # 50 条黑盒用例（禅道导入格式）
│   └── 医院门诊管理系统需求分析.xmind       # 需求/模块/流程/测试思维导图
├── init_database.py              # 一键初始化数据库（内置 DELIMITER 解析）
├── check_env.py                  # 环境自检（依赖/MySQL/数据库）
├── verify_hospital.py            # Web 端到端回归验证（黑盒冒烟）
├── gen_testcases.py              # 测试用例 Excel 生成脚本（可改后重跑）
├── gen_xmind.py                  # XMind 生成脚本
├── run_web.bat                   # 一键启动 Web
└── start_mysql.bat               # 一键启动 MySQL
```

## 三、运行步骤

### 1. 启动 MySQL

双击 `start_mysql.bat`，看到 `[OK] MySQL 已就绪（端口 3306）` 即可。
（本机 MySQL 为免服务方式，开机后需手动运行一次。）

### 2. 初始化数据库

```bash
python init_database.py
```

密码不是 123456 时：

```bash
python init_database.py --user root --password 你的密码
```

看到 `全部脚本执行成功` 即完成（建 18 张表 + 9 视图 + 16 存储过程 + 4 触发器 + 测试数据）。
已有业务数据、不想重建库时，可单独执行 08 / 09 / 10 / 11 增量脚本（幂等，不丢数据）：
```bash
python -c "from init_database import split_sql_statements; import mysql.connector; c=mysql.connector.connect(host='127.0.0.1',user='root',password='123456'); cur=c.cursor(); [cur.execute(s) for s in split_sql_statements(open('sql/09_task2_gaps.sql',encoding='utf-8-sig').read())]; print('09 增量完成')"
```

### 3. 安装依赖并启动 Web

```bash
cd web_app
python -m pip install -r requirements.txt
python app.py
```

或直接双击项目根目录 `run_web.bat`。看到 `Running on http://127.0.0.1:5000` 后，
浏览器打开 **http://127.0.0.1:5000**。

### 4. 测试账号（密码均为 123456）

| 账号 | 角色 | 可测试功能 |
| --- | --- | --- |
| admin01 | 管理员 | 全部功能 + 用户管理（注册新账号/重置密码/启停用/删除）+ 科室管理 + 公告管理 |
| reg01 | 挂号员 | 患者建档、挂号、退号、排班、预约挂号（提前预约/取号/取消） |
| regm01 | 挂号机 | 自助挂号终端：身份证识别、未建档快速建档、自助挂号（仅终端功能，不开放内部页面） |
| doctor01 | 医生 | 查看挂号、接诊/完成就诊、开具处方、体征记录、检验申请、诊断管理 |
| cash01 | 收费员 | 收费、退费、检验收费、药品目录、报表 |
| lab01 | 检验技师 | 检验申请受理、结果录入、检验汇总、报表 |
| pharm01 | 药房人员 | 处方发药、药品目录、报表 |

## 四、演示路径（建议按此顺序体验完整业务闭环）

1. **admin01** → 用户管理为各科室工作人员注册账号（医生角色自动建档）
2. **reg01 登录** → 患者档案新增一位患者 → 挂号管理办理挂号（选今日排班）→ 看到排队号
3. **reg01** → 预约挂号：识别患者 → 选明日/后日排班 → 提交生成预约流水号与挂号时限（上午 11:00 / 下午 17:00 前取号）→ 可取消预约释放号源 → 就诊当日点"取号"转正式挂号并收费（当日取号可先在挂号管理/自助终端挂号，预约作为提前占号）
4. **doctor01 登录** → 诊断管理填写诊断 → 处方开具选择该挂号 → 添加药品 → 提交
5. **doctor01** → 体征记录录入患者体温/血压/心率 → 检验管理开具检验申请（自动按项目单价计算费用）
6. **lab01** → 检验管理点"录入结果"填各项目结果 → 全部出结果后单状态自动变"已完成"
7. **cash01 登录** → 收费退费：对待收费处方收费（扣库存）→ 对待收费检验单收费（检查费）→ 退费（恢复库存）
8. **pharm01 登录** → 发药管理：对已收费处方发药（复核库存后置"已发药"）
9. **admin01** → 报表查询查看各统计、CSV 导出

## 五、自动化验证

```bash
python verify_hospital.py
```

回归核心业务流：登录 → 各页面 → 挂号 → 开方 → 收费 → 退费 → 退号 → 发药 → 检验收费 → 诊断 → 用户注册，全部 PASS 即健康。
（会向数据库写入一笔演示数据，如需干净数据重新执行 init_database.py 即可。）

## 六、数据库设计要点（课程要求覆盖）

- **表**：18 张（原 17 张 + 公告表 announcements）
- **公告模块**：管理员发布/删除公告（is_published 控制上线下线），全员可在“公告”页与首页公告栏查看
- **科室管理**：管理员维护科室字典（编码/名称/简介），展示各科室医生数与用户数，有医生/用户的科室不可删除
- **预约挂号模块**：提前预约未来排班 → 生成预约流水号与挂号时限（就诊日上午 11:00 / 下午 17:00 前取号）→ 就诊当日取号转正式挂号并收费；已挂号 + 有效预约共享号源池（预约不超号源上限，取号时排除自身预约），状态机 已预约 → 已取号 / 已取消，逾期未取不占号源并展示"已过期"；排班视图新增"已约"列（remain_count 自动扣除有效预约）
- **检验科模块**：检验申请（按项目单价自动计费）→ 检验结果录入 → 状态自动流转（待检验→检验中→已完成）、按患者汇总视图、异常结果统计
- **诊断模块**：患者档案携带诊断字段，医生可添加/修改，随病历闭环
- **药房发药**：处方状态机"待收费→已收费→已发药"，发药时复核库存并写操作日志
- **检验计费**：检验单开具时按项目单价汇总 total_fee，收费记录以"检查费"类型关联检验单，防重复收费
- **用户管理**：超级用户通过存储过程注册挂号员/医生/收费员/检验技师/药房人员账号，医生自动建档案
- **视图**：9 个（原 5 个 + 检验单列表含费用与收费状态、检验结果明细、按患者检验汇总、体征历史）
- **存储过程**：16 个（原 13 个 + 创建预约、取消预约、预约取号；挂号存储过程升级为号源校验含有效预约），全部使用事务
- **触发器**：4 个（挂号/收费/建档自动写日志、有挂号的排班禁止停诊）
- **约束与索引**：唯一约束（病历号/身份证/电话/排班）、检查约束、高频查询索引
- **查询示例**：sql/06 中多表联查、分组统计、子查询

## 七、禅道与测试用例使用

1. 打开禅道 → 测试 → 用例 → 导入；
2. 选择 `docs/黑盒测试用例_医院门诊管理系统.xlsx`；
3. 字段映射：所属模块 / 用例标题 / 前置条件 / 步骤 / 预期 / 优先级 / 用例类型 / 适用阶段 / 关键词；
4. 导入后在禅道中执行用例、关联需求、记录缺陷。

## 八、常见问题

| 问题 | 处理 |
| --- | --- |
| 页面打不开 | 确认 `python app.py` 正在运行，不要关闭运行窗口 |
| 无法连接数据库 | 运行 `python check_env.py` 定位；确认 start_mysql.bat 已启动 MySQL |
| 登录失败 | 用户名不带引号；密码默认 123456；停用账号无法登录（管理员在用户管理恢复） |
| 号源已满 | 换排班或等退号；可在排班页加大号源数；预约占用号源，取消后释放 |
| 取号失败 | 仅限就诊当日且未过挂号时限才能取号；超时限预约自动失效不占号源（列表显示"已过期"） |
| 重复预约提示 | 同一患者在同一排班只能有一条有效预约，先取消旧预约或换排班 |
| 排班不能停诊 | 该排班已有挂号，需先退号（触发器保护） |
| 库存不足开不了方 | 药品目录页补库存或改小数量 |
| 发药失败 | 处方必须已收费；药品库存不足时先补库存 |
| 检验收费失败 | 已收费检验单不可重复收费；已作废检验单不可收费 |
| 端口 5000 被占用 | 关闭其他占用 5000 的程序后重启 |

## 九、近期新增功能（毕设论文对应模块）

1. **公告管理**：admin01 登录 → 菜单“公告”→ 发布/删除公告；所有角色菜单均有“公告”入口；首页看板顶部展示最新 3 条公告。
2. **科室管理**：admin01 登录 → 菜单“科室管理”→ 新增/行内编辑/删除科室（统计各科室医生数、用户数）。
3. **患者端查询（自助终端）**：regm01 登录 → 识别患者身份证 → 页面向下滚动可查看“就诊记录/缴费记录/检验报告单”（历史挂号、缴费单、检验结果明细）。
4. **医生接诊/完成就诊**：doctor01 登录 → 处方开具页顶部“今日就诊队列”→ 开始接诊（状态：待就诊→就诊中）→ 完成就诊（状态：就诊中→已就诊）。
5. **数据库**：新增 `sql/11_announcement.sql`（announcements 表 + 2 条演示公告）；`init_database.py` 已纳入（一键执行 sql/01~11）。
